<Window x:Class="ImageViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:ImageViewer"
        Title="Просмотр изображений" Height="620" Width="640" Background="White">
    <Window.Resources>
        <!-- выделенный элемент отображается над остальными -->
        <Style TargetType="local:ImageCard">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Panel.ZIndex" Value="99" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <WrapPanel Margin="10">
            <local:ImageCard Source="Images/1.png" Title="Море" Margin="10" />
            <local:ImageCard Source="Images/2.png" Title="Лес" Margin="10" />
            <local:ImageCard Source="Images/3.png" Title="Закат" Margin="10" />
            <local:ImageCard Source="Images/4.png" Title="Горы" Margin="10" />
        </WrapPanel>
    </ScrollViewer>
</Window>
